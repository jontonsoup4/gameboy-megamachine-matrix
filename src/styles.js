import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
  container: {
    margin: '0 auto',
  },
  gameboys: {
    margin: theme.spacing(0.5),
    width: 'calc(100% - 4px)',
  },
}));
